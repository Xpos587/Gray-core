{
  "name": "gray-core-docs",
  "version": "1.0.0",
  "description": "Gray-core Documentation",
  "scripts": {
    "docs:dev": "cd docs && vitepress dev",
    "docs:build": "cd docs && vitepress build",
    "docs:preview": "cd docs && vitepress preview --port 5173"
  },
  "devDependencies": {
    "vitepress": "^1.5.0"
  }
}
